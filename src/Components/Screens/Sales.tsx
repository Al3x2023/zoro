import React from 'react';
import { View } from 'react-native';
import { Sale } from './Sale'; // Importa el componente

export const Sales=()=>{
  return (
    <View style={{ flex: 1 }}>
      <Sale />
    </View>
  );
}
